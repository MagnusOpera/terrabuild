[<export>] let run (context: {| Command: string |}) = "run"

type ExportFlag =
  | Dispatch
  | Default
  | Batchable
  | Never
  | Local
  | External
  | Remote

{ [nameof run] = [Batchable; Local] }
