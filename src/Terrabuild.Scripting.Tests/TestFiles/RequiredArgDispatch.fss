export let dispatch (context: { Command: string }) (requiredArg: string) =
  requiredArg

type ExportFlag =
  | Dispatch
  | Default
  | Batchable
  | Never
  | Local
  | External
  | Remote

Map.ofList [(nameof dispatch, [Dispatch])]
