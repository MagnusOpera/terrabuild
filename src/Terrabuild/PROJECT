
project terrabuild {
    labels = [ "app"
               "dotnet"]

    @dotnet { }
}

target build {
    @dotnet restore { dependencies = true }
    @dotnet build { version = terrabuild.tag }
}

target dist {
    outputs = []

    @dotnet restore { dependencies = true }
    @dotnet pack { }
}
