
project terrabuild {
    labels = [ "app"
               "dotnet"]
    depends_on = [ project.terrabuild_ui ]
    @dotnetsdk { }
}

target install {
    @dotnetsdk restore { }
}

target build {
    @dotnetsdk build { }
}

target dist {
    outputs = []

    @dotnetsdk publish { single = true
                      build = true
                      restore = true }
}
