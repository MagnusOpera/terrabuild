
project {
    labels = [ "app"
               "dotnet"]

    @dotnet { }
}

target build {
    @dotnet restore { }
    @dotnet build { version = terrabuild.tag }
}

target dist {
    outputs = []

    @dotnet restore { }
    @dotnet pack { }
}
