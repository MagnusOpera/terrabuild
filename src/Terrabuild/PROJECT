
project terrabuild {
    labels = [ "app"
               "dotnet"]

    @dotnet { }
}

target install {
    @dotnet restore { }
}

target build {
    @dotnet build { }
}

target dist {
    outputs = []

    @dotnet publish { single = true
                      build = true
                      restore = true }
}
