
project {
    labels = [ "app"
               "dotnet"]

    @dotnet { }
}

target install {
    @dotnet restore { }
}

target build {
    @dotnet build { version = terrabuild.tag }
}

target dist {
    outputs = []
    @dotnet pack { }
}
