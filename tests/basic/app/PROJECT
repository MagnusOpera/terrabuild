
project appprj {
    outputs = [ "dist" ]
    labels = [ "app" ]
    environments = [ "dev*" ]

    @shell { }
}

target build {
    @shell echo { args = "building app with lib version: ${project.libprj.version}" }
}
