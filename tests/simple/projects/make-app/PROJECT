
project {
    depends_on = [ project.shell_lib ]
    outputs = [ "dist" ]
    labels = [ "app" ]
}

target build {
    @shell echo { args = "building project1" }
    @make build { variables = { secret: local.secret_message } }
}
