extension @docker {
    defaults {
        image = "ghcr.io/magnusopera/dotnet-sdk"
    }
}

project dotnet_sdk {
    labels = [ "toolchain" ]
    @docker { }
}

target dist {
    phase = phase.toolchains
    @docker build { build_args = { dotnet: local.versions.dotnet_sdk } }
}
